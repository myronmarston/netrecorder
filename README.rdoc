My fork of NetRecorder went in a significantly different direction than the original, and would up with an almost
entirely new codebase.  So I decided to release it as a separate gem, {VCR}[http://github.com/myronmarston/vcr].